<layout>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical"
        android:background="@color/dark_background_question_list"
        tools:context="com.devfie.thoth.messaging.MessagingFragment">

        <!-- TODO: Update blank fragment layout -->

        <android.support.v7.widget.RecyclerView
            app:layoutManager="LinearLayoutManager"
            android:id="@+id/recyclerMessaging"
            android:layout_weight="1"
            android:layout_width="match_parent"
            android:layout_height="0dp" />

        <LinearLayout
            android:background="@color/white"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageButton
                android:id="@+id/btnMsgAttach"
                android:padding="@dimen/margin_form_little"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:srcCompat="@drawable/ic_add_black_24dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <EditText
                android:id="@+id/edtMsgContent"
                android:textColor="@color/dark_text_color"
                android:textColorHint="@color/middle_grey"
                android:textSize="13sp"
                android:hint="@string/content_write_message"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:paddingEnd="@dimen/margin_form_little"
                android:paddingStart="@dimen/margin_form_little"
                android:background="@color/transparent"
                android:layout_weight="1"
                android:layout_width="0dp"
                android:layout_height="wrap_content" />

            <ImageButton
                android:id="@+id/btnSendMessage"
                android:padding="@dimen/margin_form_little"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:srcCompat="@drawable/ic_send_message_24dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

        </LinearLayout>
    </LinearLayout>

</layout>
